{
  "devDependencies": {
    "style-dictionary": "^3.1.1",
    "token-transformer": "0.0.17"
  },
  "dependencies": {},
  "scripts": {
    "build": "token-transformer figma-sync/all.json transformer-output/moj.json moj --expandTypography=true && token-transformer figma-sync/all.json transformer-output/nav-cat.json 'nav&cat' --expandTypography=true && node build.js"
  }
}
